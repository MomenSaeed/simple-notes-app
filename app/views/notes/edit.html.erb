<h1>Editing Note</h1>

<%= render 'form', note: @note %>

<%= link_to 'Show', user_note_path(@note.creator_id, @note) %> |
<%= link_to 'Back', user_notes_path(@note.creator_id) %>

<br/>
<br/>
<br/>
<div>
  <h4>Current Active collaborators</h4>

  <table>
    <thead>
      <tr>
        <th>Email</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody id="note-collaborators-table-<%= @note.try(:id) %>" data-note-id="{}">
    </tbody>
  </table>

  <br>
</div>
